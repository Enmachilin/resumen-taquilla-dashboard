import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.resumentaquilla.analitica',
  appName: 'Data Park Admin',
  webDir: 'out'
};

export default config;
